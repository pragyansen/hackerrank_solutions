using System;
using System.Collections.Generic;
using System.Text;

namespace hackerrank_nCr
{
    class Program
    {
        static void Main(string[] args)
        {
           /* int ceiling = 1000000;
            List<int> dividers = new List<int>();
            List<int> semilist = new List<int>();
            for (int i = 2; i <= ceiling / 2; i++)
            {
                dividers.Add(i);
            }
            int p = 2;
            while (Math.Pow(p, p) < ceiling)
            {
                p = dividers[0]; dividers.Remove(p); semilist.Add(p);
                for (int i = 0; i < dividers.Count; i++)
                    if (dividers[i] % p == 0)
                        dividers.Remove(dividers[i]);
            }
            for (int i = 0; i < dividers.Count; i++)
            {
                semilist.Add(dividers[i]);
            }

            foreach (int i in semilist)
                Console.WriteLine(i);
            Console.ReadKey();
            */


        }
    }
}
